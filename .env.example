# Optional: server port (hosts like Render set PORT automatically)
PORT=3000

# Optional: session secret (use a long random string in production)
SESSION_SECRET=your-secret-key-change-in-production

# Optional: set for production
NODE_ENV=production
